const menu=document.querySelector('.menu');menu.addEventListener('click',()=>menu.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')));
document.getElementById('quoteForm').addEventListener('submit',e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));const subject=encodeURIComponent(`Richiesta preventivo: ${d.partenza} → ${d.destinazione}`);const body=encodeURIComponent(`Partenza: ${d.partenza}\nDestinazione: ${d.destinazione}\nData: ${d.data||'-'}\nServizio: ${d.servizio}\nPeso: ${d.peso||'-'}\nColli/Pallet: ${d.colli||'-'}\n\nNote:\n${d.note||'-'}`);document.getElementById('formMsg').textContent='Apro il tuo programma email con la richiesta già compilata.';location.href=`mailto:traffico@transportserviceitalia.it?subject=${subject}&body=${body}`;});
if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js');
