(()=>{const btn=document.querySelector('.menuBtn'),nav=document.querySelector('#mainNav');const close=()=>{nav?.classList.remove('open');btn?.setAttribute('aria-expanded','false')};btn?.addEventListener('click',()=>{const on=nav.classList.toggle('open');btn.setAttribute('aria-expanded',String(on))});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');if(id==='#')return;const t=document.querySelector(id);if(t){e.preventDefault();close();t.scrollIntoView({behavior:'smooth',block:'start'});history.replaceState(null,'',id)}}));const secs=[...document.querySelectorAll('main section[id],header[id]')],links=[...document.querySelectorAll('.mainNav a')];const io=new IntersectionObserver(es=>{const v=es.filter(x=>x.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!v)return;links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+v.target.id))},{rootMargin:'-25% 0px -60%',threshold:[0,.2,.5]});secs.forEach(s=>io.observe(s));const f=document.querySelector('#quoteForm');f?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(f),s=`Richiesta preventivo NTSE - ${d.get('from')} > ${d.get('to')}`,b=`Partenza: ${d.get('from')}\nDestinazione: ${d.get('to')}\nMerce: ${d.get('goods')}\nData: ${d.get('date')||'-'}\n\nNote:\n${d.get('notes')||'-'}`;location.href=`mailto:traffico@transportserviceitalia.it?subject=${encodeURIComponent(s)}&body=${encodeURIComponent(b)}`})})();
